<%= form_with(model: denounce_type, local: true) do |form| %>
  <% if denounce_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(denounce_type.errors.count, "error") %> prohibited this denounce_type from being saved:</h2>

      <ul>
      <% denounce_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
